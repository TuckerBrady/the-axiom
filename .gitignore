node_modules/
.expo/
dist/
build/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.orig
.claude/*
!.claude/commands/
!.claude/commands/**
CLAUDE.local.md

# Test coverage
coverage/
*.lcov

# Maestro
.maestro/.env

# Driven screenshot harness (PROMPT_159). Runs are disposable by default —
# nobody reviews a pull request full of PNGs. The one exception is the
# board-size standard set, committed once under
# project-docs/SHOTS/board-size-standard/.
__shots__/
# Compiled output of `npm run shots`.
.shots-build/

# EAS
google-services-key.json
*.p8
*.p12
*.mobileprovision

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Auto-generated build info — regenerated on postinstall/prestart
src/buildInfo.ts

# Cowork-to-Code handoff prompts — drafted by Cowork, referenced
# via /prompt slash command. Not part of game codebase history.
cowork-prompts/
# Native projects are generated by `expo prebuild` (CNG). The Android run for
# AXM-011 generates ./android on demand; it is build output, not source.
/android/
/ios/
