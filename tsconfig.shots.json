{
  "//": "Build config for the `npm run shots` harness only. The app's own tsconfig targets React Native; the harness is a Node CLI, so it compiles to CommonJS into a gitignored .shots-build/ and runs from there. rootDir is src/, which puts the entry at .shots-build/shots/cli.js — two levels below the repo root, which is what cli.ts assumes.",
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020"],
    "module": "commonjs",
    "moduleResolution": "node",
    "rootDir": "src",
    "outDir": ".shots-build",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "types": ["node"]
  },
  "include": ["src/shots/**/*.ts", "src/utils/boardSizeOverride.ts"]
}
